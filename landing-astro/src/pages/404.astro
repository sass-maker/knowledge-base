<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <meta name="robots" content="noindex, nofollow" />
    <title>Page not found | Knowledge Base</title>
  </head>
  <body>
    <main>
      <a class="brand" href="/"><span aria-hidden="true">KB</span> Knowledge Base</a>
      <p>404 / Page not found</p>
      <h1>This route is outside the public boundary.</h1>
      <p class="lede">
        The public site contains one product explanation. Indexed material,
        operator settings, and retrieval access remain private.
      </p>
      <a class="button" href="/">Return to the product brief</a>
    </main>
  </body>
</html>

<style>
  :global(*) {
    box-sizing: border-box;
  }

  :global(body) {
    min-width: 320px;
    margin: 0;
    background: #000;
    color: #f5f7f6;
    font-family: ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
  }

  main {
    width: min(54rem, calc(100% - 2rem));
    margin: 0 auto;
    padding: clamp(2rem, 6vw, 5rem) 0;
  }

  .brand {
    display: inline-flex;
    align-items: center;
    gap: 0.75rem;
    min-height: 2.75rem;
    color: inherit;
    font-weight: 700;
    text-decoration: none;
  }

  .brand span {
    display: inline-grid;
    width: 2.1rem;
    height: 2.1rem;
    place-items: center;
    border-radius: 0.7rem;
    background: #08cbb6;
    color: #001b17;
    font: 800 0.72rem/1 ui-monospace, monospace;
  }

  main > p:first-of-type {
    margin: clamp(5rem, 14vw, 10rem) 0 1rem;
    color: #79cfc4;
    font: 700 0.75rem/1.4 ui-monospace, monospace;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  h1 {
    max-width: 49rem;
    margin: 0;
    font-size: clamp(3rem, 8vw, 6rem);
    line-height: 0.98;
    letter-spacing: -0.045em;
  }

  .lede {
    max-width: 39rem;
    margin: 1.5rem 0 2rem;
    color: #aeb4b1;
    font-size: 1.08rem;
    line-height: 1.65;
  }

  .button {
    display: inline-flex;
    min-height: 3rem;
    align-items: center;
    padding: 0.8rem 1.25rem;
    border-radius: 0.55rem;
    background: #08cbb6;
    color: #001b17;
    font-weight: 700;
    text-decoration: none;
  }

  :focus-visible {
    outline: 3px solid #84fff0;
    outline-offset: 3px;
  }
</style>
